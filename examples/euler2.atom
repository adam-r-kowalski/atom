problem2 = fn(n: i32, sum: i32, prev: i32, curr: i32) i32 {
    if curr > n {
        sum
    } else {
        multiple = curr % 2 == 0
        delta = if multiple { curr } else { 0 }
        problem2(n, sum + delta, curr, prev + curr)
    }
}

start = fn() i32 {
	problem2(4000000, 0, 0, 1)
}
